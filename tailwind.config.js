/** @type {import('tailwindcss').Config} */
export default {
    darkMode: 'class', // Enables class-based dark mode
    content: [
        './src/**/*.{html,js,svelte,ts}', // Adjust if your file paths are different
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}
